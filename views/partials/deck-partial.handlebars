{{!-- <div class="card text-center col-12 col-md-4 mx-2 px-0">
    <div class="card-header">
        <h3>{{title}}</h3>
    </div>
    <div class="card-body">
        <p class="card-text">
            {{cards.length}} {{format_plural "card" cards.length}} in this Deck
        </p>
        <p class="card-text">
            Created on {{format_date created_at}}
        </p>
        <a href="/decks/{{id}}" class="btn btn-primary">Edit Deck</a>
        <button class="btn btn-primary delete-btn" data-deck-id="{{id}}">Delete deck</button>
    </div>
</div> --}}

<div class="card col-4 img-fluid">
        <img src="/assets/imgs/project-2-bg.png" class="card-img-top" alt="back-and-orange-swirl" style="width:100%">
      <div class="card-img-overlay d-block mx-auto">
        <h1 class="text-light">{{title}}</h1>
        <p class="card-text text-light">{{user.username}}  |  {{cards.length}} {{format_plural "card" cards.length}}  |  <a href="/deck/{{id}}" class="">view deck</a></p>
      </div>
</div>